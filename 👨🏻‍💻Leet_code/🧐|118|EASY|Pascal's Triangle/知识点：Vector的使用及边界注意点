# 题目定义：
Pascal's Triangle问题，给定一个非负数的值numRows,算出Pascal's Triangle。

# 题目解析
第一行直接插入vector<int>{1}到返回vector中，
后面每一行都是前一行从 (i = -1) 到 (i = 前一行数组大小 - 1) 插入 (前一行数组[i] + 前一行数组[i + 1])

* 我们将 前一行数组[-1] 的值定为0, 前一行数组[前一行数组大小] 的值定为0.

## Kai的解法和发现
